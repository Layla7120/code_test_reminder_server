# 작업중 — 약점 체크리스트

> 이 파일은 **임시**입니다. 전부 지워지면 파일도 삭제합니다.
> 항목을 지울 때는 반드시 **증명 테스트 파일명**을 옆에 적으세요.
> 체크리스트는 사라지고 그 내용은 테스트로 남습니다.

멈추는 조건: 아래 8개 테스트가 돌고, A가 고쳐졌고, 측정 하나가 결과 파일과 함께 있고,
이야기 세 개를 종이 없이 답할 수 있으면 **손 뗀다.**

---

# 현재 상태 (2026-07-29)

## 무엇을 하려는가

**목표는 "코드를 좋게 만드는 것"이 아니라 "주장을 증명 가능하게 만드는 것"입니다.**
지금 이 프로젝트는 결함 11개를 고쳤다고 주장하는데 증명이 하나도 없습니다.

세 가지를 만들면 끝입니다 — ① 주장마다 붙은 테스트 8개, ② 변수 하나만 바꾼 측정 하나,
③ 사실과 맞는 문서 하나. 그리고 **이야기 세 개**를 종이 없이 답할 수 있으면 손 뗍니다.

## 진행률 — **25%**

```
분석·설계  ████████████████████ 100%   (비중 15%)
0단계      ████████████████████ 100%   (비중 10%)
1단계      ░░░░░░░░░░░░░░░░░░░░   0%   (비중 10%)  ← 다음
2단계      ░░░░░░░░░░░░░░░░░░░░   0%   (비중 20%)  ← 제일 중요
3단계      ░░░░░░░░░░░░░░░░░░░░   0%   (비중 20%)
4단계      ░░░░░░░░░░░░░░░░░░░░   0%   (비중 15%)
5단계      ░░░░░░░░░░░░░░░░░░░░   0%   (비중 10%)
```

| 단계 | 무엇 | 상태 |
|---|---|---|
| 분석·설계 | 급조 부분 찾기, Flask/Kotlin 워크스루, 스키마 재설계 | **완료** |
| 0단계 | 테스트를 돌릴 수 있게 만들기 | **완료** (테스트 4개 통과) |
| 1단계 | 첫 테스트 하나 (동시 그룹 참여) | 미착수 ← **다음** |
| 2단계 | 버그 A 재현 후 수정 | 미착수 |
| 3단계 | 나머지 테스트 채우기 | 미착수 |
| 4단계 | 측정 하나 | 미착수 |
| 5단계 | 문서 고치기 | 미착수 |

**코드 변경: 0줄.** 아직 `server/`, `app/`, `infra/` 어디도 건드리지 않았습니다.
지금까지 만든 건 이 파일과 [스키마-재설계.md](스키마-재설계.md) 두 개뿐입니다.

> 한 단계 끝날 때마다 위 진행률을 갱신합니다.

## 도중에 방향이 바뀐 것 (기억해 둘 것)

- **다중 그룹** — "스키마가 과하다"고 봤는데 **요구사항이 맞고 앱이 버그**였음.
  `GroupService.kt:71`의 `participations.first()` → 3단계 테스트 항목으로 등록
- **`member_counter`** — 재설계 초안에서 제거했다가 **되돌림.**
  락+COUNT도 정확하지만 정원 불변식이 전부 앱으로 옮겨감
- **Flyway** — 안 넣기로. 스키마 변경이 범위에 없고, Flyway도 DDL은 손으로 씀
- **`validate`에 대한 오해** — Hibernate `validate`는 컬럼·타입만 보고
  **UNIQUE·인덱스는 검증하지 않음.** 그래서 지금 엔티티와 `init.sql`이 두 군데 이미 어긋나 있음

## 아직 결정 안 한 것

- [ ] **커밋 기록 처리** — 커밋에 "토스페이먼츠"는 없음. `6320750` 제목과 `14347b2` 본문에
      "자소서"만 있음. 전체 삭제보다 **메시지 2개 수정**을 권함. 공개 리포라 force push 필요 → **마지막에**
- [ ] **스키마 6줄 적용 시점** — 0단계 이후를 권함 (테스트가 있어야 뭐가 깨지는지 보임)
- [ ] **`CLAUDE.md` 생성** — 다음에 다시 스캔 안 하려면 필요

---

## 0단계 — 기반 (기능 코드 손대지 않음)

- [x] `build.gradle.kts`에 Testcontainers 추가 — **모듈명 주의**: 2.x부터
      `testcontainers-mysql`, `testcontainers-junit-jupiter` (1.x의 `mysql`, `junit-jupiter` 아님).
      버전은 Spring Boot 4.0.4 BOM이 관리 (2.0.4)
- [x] `application-test.yml` 분리 → `src/test/resources/`
- [x] MySQL 컨테이너에 `infra/init.sql` 물리기 — 복사본 없이 원본 참조.
      경로는 Gradle Test 태스크가 `schema.init.sql` 시스템 프로퍼티로 전달
- [x] MySQL·Redis 컨테이너 기동 확인 → `support/ContainerSmokeTest.kt` **3개 통과**

**결과: `./gradlew test` 4개 통과 (스킵 0).** `docker-compose`를 손으로 켤 필요 없음.

> **실행 시 주의**: 이 맥은 자바가 Homebrew keg-only로 깔려 있어 PATH에 안 잡힙니다.
> ```
> export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
> cd server && ./gradlew test
> ```

> **0단계 이전에 정말 안 돌았는가** — 확인함. 원래 설정 그대로 재현해 실행하니
> `java.net.ConnectException`(MySQL 연결 실패)로 죽음. 있던 테스트는
> `contextLoads()` 하나였고 본문이 비어 있어 아무것도 검증하지 않았음.
> `.env`도 이 폴더에 없음.

**H2 / 임베디드 Redis 금지.** 검증하려는 대상이 DB 엔진과 Redis의 실제 동작이라 대체하면 테스트가 의미를 잃습니다.

**테스트 프로파일에 `ddl-auto: create-drop`도 금지.** 같은 이유입니다.
검증 대상이 `uk_participate_group_user` UNIQUE 제약과 InnoDB row lock인데,
Hibernate가 생성한 스키마는 운영(`infra/init.sql`)과 미묘하게 다를 수 있습니다.
`validate` 유지 + 운영과 **같은 init.sql**을 씁니다.

### 잠복 — 스키마를 손으로 관리하고 있음

`ddl-auto: validate`는 스키마를 만들어주지 않으므로, 엔티티를 바꾸면 `infra/init.sql`을
손으로 같이 고쳐야 하고 안 고치면 부팅이 막힙니다. 이미 두 번 물린 흔적:
`b20f993`(init.sql을 엔티티에 맞게 수정), `f4166dc`(rank 예약어 백틱).

또 docker-compose의 init.sql은 **볼륨이 비어 있을 때 최초 1회만** 실행됩니다.
`mysql-data` 볼륨이 남아 있으면 init.sql을 고쳐도 반영 안 됨 → `docker compose down -v` 필요.

- [ ] 문서 `:29`가 "마이그레이션: Flyway"라고 써 있으나 **Flyway는 들어가 있지 않음** → 5단계에서 교정
- [ ] Flyway 도입 자체는 **하지 않음** (멘토: 기술 스택 늘리지 말 것). 이 항목은 "알고 있으나 안 한 선택"으로 남김

---

## 1단계 — 첫 테스트 하나

- [ ] **동시 joinGroup**: 정원 1자리에 N명 동시 요청 → 정확히 1명만 성공
      - 대상: `GroupRepository.incrementMemberCounterIfNotFull()`
      - 이게 돌면 나머지 테스트는 복제하면 됩니다
      - 증명 테스트: `______________________`

---

## 2단계 — 버그 A (핵심)

### 무슨 일이 일어나고 있나

커밋을 가져올 때 서버가 이 순서로 일합니다.

1. 깃허브에서 커밋 **5개**를 받아온다
2. DB에 저장한다 — **이미 있는 건 안 넣는다**
3. 랭킹 점수를 올린다 — **그런데 그냥 "5점"을 더한다**

2번과 3번이 따로 놉니다. 2번에서 실제로 **0개**만 들어갔어도 3번은 5점을 더합니다.
버튼을 세 번 누르면 DB는 커밋 5개 그대로인데 랭킹은 **15점**이 됩니다.

### 스케줄러가 왜 안 고쳐주나

매시간 도는 스케줄러는 "DB를 세어보고 랭킹 점수를 맞추는" 일을 하는데,
**점수를 올리기만 하고 내리지는 않게** 되어 있습니다.

그렇게 만든 이유가 있습니다. 스케줄러가 DB를 세는 동안 사용자가 새 커밋을 올리면,
스케줄러가 방금 오른 점수를 옛날 숫자로 덮어써서 없애버립니다.
그걸 막으려고 **"지금 점수보다 클 때만 바꾼다"** 는 규칙을 넣었습니다.

```
랭킹 점수: 15점
DB 실제:    5개

스케줄러: "5는 15보다 작네. 안 바꿔."
→ 15점 그대로. 영원히.
```

**안전장치 두 개가 서로를 막고 있습니다.** 하나는 점수를 부풀리고, 다른 하나는 그걸 못 내리게 합니다.

### 왜 지금까지 몰랐나

부하 테스트용 가짜 깃허브가 **매번 새 커밋을 만들어냈습니다.**
중복이 생길 수가 없으니 이 버그가 나타날 방법이 없었습니다.

### 고치는 법

3번에서 "5개"가 아니라 **"실제로 들어간 개수"** 를 더합니다.
그러면 점수가 애초에 안 부풀어서 스케줄러가 내릴 일도 없어집니다.
**규칙("올리기만 한다")은 그대로 둡니다.** 그게 틀린 게 아니라 틀린 숫자를 넣은 게 문제였으니까요.

> 면접에서 강한 이유: "버그를 찾았다"가 아니라
> **"내가 넣은 안전장치 두 개가 서로를 방해하는 걸 발견했다"** 이기 때문입니다.
> 하나는 남이 봐도 찾지만, 둘의 상호작용은 자기 설계를 이해해야 보입니다.

---

**먼저 빨간불로 재현하고, 그 다음에 고칩니다.**

- [ ] **A-1. Mock을 실제와 같게** — `MockGithubClient.kt:41`이 매번 새 UUID sha를 뱉어서
      중복이 구조적으로 발생하지 않음 → 이 버그가 100% 가려졌음. 고정 sha 시드 기반으로 교체
- [ ] **A-2. 빨간불 재현** — 같은 커밋 목록으로 두 번 수집 → Redis 점수가 2배가 되는 것 확인
- [ ] **A-3. 수정** — `CommitService.kt:45-52`가 `dtos.size`(요청 개수)를 증분으로 발행.
      `bulkUpsert`는 `ON DUPLICATE KEY UPDATE`라 실제 삽입은 0건일 수 있음.
      → **실제 삽입 건수**를 반환하도록 변경
      - 주의: MySQL `rewriteBatchedStatements` 하에서 `batchUpdate` 반환값은 `SUCCESS_NO_INFO(-2)`.
        배치 결과로 건수를 세면 안 됩니다. 삽입 대상 sha 사전 조회 방식 권장
- [ ] **A-4. 월 버킷** — 버킷이 `YearMonth.now(clock)`인데 `commitDate`는 과거일 수 있음.
      월초 수집 시 지난달 커밋이 이번달 ZSET에 가산됨 → `commitDate` 기준으로 월별 분할 발행
      - 증명 테스트: `______________________`

- [ ] **A-5. 회귀 방지 테스트** — "점수를 못 내린다"는 사실 자체를 테스트로 고정.
      나중에 누가 규칙을 되돌리지 못하게. 해당 코드: `RankingRedisRepository.kt:26-32`
      - 증명 테스트: `______________________`

---

## 3단계 — 나머지 테스트 (8개 채우기)

- [ ] **스케줄러 정합성**: 스케줄러가 돌고 나면 Redis 점수 = DB 실제 개수
      - 증명 테스트: `______________________`
- [ ] **leaveGroup 카운터 드리프트** — `GroupService.kt:57-63`.
      삭제 0건이어도 `decrementMemberCounter`를 무조건 호출 →
      비멤버의 탈퇴 요청으로 카운터가 내려가 정원 방어가 뚫림
      - 증명 테스트: `______________________`
- [ ] **분산 락 해제 순서** — `CommitService.kt:53-55`.
      락 해제가 `@Transactional` 메서드 내부 `finally` → **커밋 전에 락이 풀림**.
      그 틈의 두 번째 요청이 GitHub을 중복 호출. 또 소유권 검증 없이 `delete`
      - 증명 테스트: `______________________`
- [ ] **Redis 장애 폴백** — `RankService.kt:23,32`가 `RedisConnectionFailureException`만 catch.
      타임아웃(`QueryTimeoutException`)은 그대로 500.
      `getUserRank`는 정상 경로에서 `null`이 나와도 DB 폴백을 안 함
      → **"Redis가 죽으면요?"에 대한 답을 여기서 만듭니다**
      - 증명 테스트: `______________________`
- [ ] **Clock 고정 테스트** — `Clock.fixed()`로 특정 시점 재현.
      전환의 핵심 성과인데 정작 쓰는 테스트가 없음
      - 증명 테스트: `______________________`
- [ ] **다중 그룹 참여가 조용히 잘림** — `GroupService.kt:71`의 `participations.first()`.
      다중 참여는 **요구사항**인데 첫 그룹만 반환함 → 스키마가 아니라 **앱이 버그**.
      두 그룹에 가입한 유저의 조회가 목록을 반환하는지
      - 증명 테스트: `______________________`

### 테스트 없이 조용히 고칠 것 (사소함)

- [ ] `RankService.kt:39-49` — `private fun`에 `@Transactional`. AOP 프록시가 못 잡아 무효
- [ ] `GroupService.kt:23-31` — `createGroup`이 `joinGroup`과 순서가 반대이고 반환값 미검사
- [ ] `@Modifying` 쿼리에 `clearAutomatically` 없음 → 같은 트랜잭션 안 엔티티가 stale (잠복)

---

## 4단계 — 깨끗한 측정 하나

**테스트와 A 수정 이후에.** 지금 재면 버그 든 상태를 재는 거라 또 버리게 됩니다.

- [ ] 변수 **하나만** 바꾼 A/B: **Redis 랭킹 on/off** (인덱스보다 프로젝트 서사에 직결)
- [ ] 절대값이 아니라 **규모를 바꿔가며 기울기**: 유저 1만 / 5만 / 10만
- [ ] 표 하나 + 결과 파일
      - 결과 파일: `______________________`

기존 `k6_result*.json` 3개는 변수가 최소 4개(커넥션 풀 15 vs 20, 캐시 커버리지, 시나리오 구성,
처리량 14,677 vs 5,081) 섞여 있어 A/B가 아닙니다. **새로 잽니다.**

---

## 5단계 — 문서 하나

`docs/spring-migration-plan.md` (445줄) 재작성:

- [ ] 자소서 문단 삭제 (`:3`, `:320-322`)
- [ ] 면접 답변 블록 5개 + 예상 질문 표 9행 삭제 (`:433-445`)
- [ ] **시점 교정** — 결함 4(인덱스 무력화)·5(NOW 하드코딩)는 **Flask 단계에서 고친 것**.
      커밋 `0bd802f`(2026-03-15)에 이미 적혀 있고, 지금 `commit_service.py:60-115`는 이미 범위 비교임.
      `:142-153`이 인용한 `WHERE YEAR(commit_date)`는 **현재 코드에 없는 코드**임
- [ ] **p95 서사 교정** — Flask p95 8.61ms는 29.51%가 빠르게 5xx로 떨어져서 낮게 나온 숫자.
      Spring 5.805ms와 나란히 놓으면 안 됨. 진짜 차이는 **실패율**.
      `:11`의 "4,234ms → 86ms"도 Flask 자체 튜닝 전후 수치라 마이그레이션 성과가 아님
- [ ] 각 결함 옆에 **증명 테스트 링크**를 걸어 문서와 코드를 묶기

---

## 이야기 세 개 (꼬리질문 3개를 버티는가)

1. **Redis 랭킹 오프로딩** — "Redis가 죽으면요?"에 지금 답이 없음. 3단계에서 만들어야 함
2. **★ 안전장치 두 개가 서로를 무력화** — GT ↔ 하향 보정. 가장 강한 이야기.
   결함 11개 찾았다는 것보다 강함. 자기 설계의 상호작용을 본 사람이라는 증거
3. **member_counter 3단계 시행착오** — 컬럼 제거(OOM) → COUNT 후 INSERT(TOCTOU) → 원자적 UPDATE

## 얻지 말 것

- "Kotlin/Spring 할 줄 안다"는 성과가 아니라 **진입 조건**. 성과로 말하면 수준이 낮아 보임
- **기술 스택 추가 금지** — Kafka, MSA, 서킷브레이커. 넣는 순간 "왜 넣었냐"에 답해야 함
