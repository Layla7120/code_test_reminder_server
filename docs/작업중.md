# 작업중 — 약점 체크리스트

> 이 파일은 **임시**입니다. 전부 지워지면 파일도 삭제합니다.
> 항목을 지울 때는 반드시 **증명 테스트 파일명**을 옆에 적으세요.
> 체크리스트는 사라지고 그 내용은 테스트로 남습니다.

멈추는 조건 (멘토 기준):
- [x] 테스트 8개 → **26개**, Testcontainers로 실제 MySQL·Redis 사용
- [x] 랭킹 이중 계상 버그가 고쳐졌고 그걸 증명하는 테스트가 있음
- [x] 깨끗한 측정 하나가 결과 파일과 함께 있음 → [측정-결과.md](측정-결과.md)
- [ ] **이야기 세 개를 종이 없이 답할 수 있음** ← 남은 하나
      → **[면접-준비-프롬프트.md](면접-준비-프롬프트.md)** 를 다른 에이전트/새 세션에 붙여넣고 테스트.
        며칠 뒤·몇 주 뒤에 다시 돌려서 여전히 답하는지 확인할 것

마지막 항목이 되면 **손 뗀다.**

---

# 현재 상태 (2026-07-29)

## 무엇을 하려는가

**목표는 "코드를 좋게 만드는 것"이 아니라 "주장을 증명 가능하게 만드는 것"입니다.**
지금 이 프로젝트는 결함 11개를 고쳤다고 주장하는데 증명이 하나도 없습니다.

세 가지를 만들면 끝입니다 — ① 주장마다 붙은 테스트 8개, ② 변수 하나만 바꾼 측정 하나,
③ 사실과 맞는 문서 하나. 그리고 **이야기 세 개**를 종이 없이 답할 수 있으면 손 뗍니다.

## 진행률 — **100%** (코드/문서 작업 완료)

```
분석·설계  ████████████████████ 100%   (비중 15%)
0단계      ████████████████████ 100%   (비중 10%)
1단계      ████████████████████ 100%   (비중 10%)
2단계      ████████████████████ 100%   (비중 20%)
3단계      ████████████████████ 100%   (비중 20%)
4단계      ████████████████████ 100%   (비중 15%)
5단계      ████████████████████ 100%   (비중 10%)
```

**남은 것은 코드가 아닙니다** — 아래 "이야기 세 개"를 종이 없이 답할 수 있으면 손 뗍니다.

| 단계 | 무엇 | 상태 |
|---|---|---|
| 분석·설계 | 급조 부분 찾기, Flask/Kotlin 워크스루, 스키마 재설계 | **완료** |
| 0단계 | 테스트를 돌릴 수 있게 만들기 | **완료** |
| 1단계 | 첫 테스트 하나 (동시 그룹 참여) | **완료** (테스트 6개 통과) |
| 2단계 | 버그 A 재현 후 수정 | **완료** (테스트 16개 통과, 커밋 4개) |
| 3단계 | 나머지 테스트 채우기 | **완료** (테스트 25개 통과, 커밋 6개) |
| 4단계 | 측정 하나 | **완료** → [측정-결과.md](측정-결과.md) |
| 5단계 | 문서 고치기 | **완료** → [마이그레이션-기록.md](마이그레이션-기록.md) |

> 한 단계 끝날 때마다 위 진행률을 갱신합니다.

## 도중에 방향이 바뀐 것 (기억해 둘 것)

- **다중 그룹** — "스키마가 과하다"고 봤는데 **요구사항이 맞고 앱이 버그**였음.
  `GroupService.kt:71`의 `participations.first()` → 3단계 테스트 항목으로 등록
- **`member_counter`** — 재설계 초안에서 제거했다가 **되돌림.**
  락+COUNT도 정확하지만 정원 불변식이 전부 앱으로 옮겨감
- **Flyway** — 안 넣기로. 스키마 변경이 범위에 없고, Flyway도 DDL은 손으로 씀
- **`validate`에 대한 오해** — Hibernate `validate`는 컬럼·타입만 보고
  **UNIQUE·인덱스는 검증하지 않음.** 그래서 지금 엔티티와 `init.sql`이 두 군데 이미 어긋나 있음

## 아직 결정 안 한 것

- [ ] **커밋 기록 처리** — 커밋에 "토스페이먼츠"는 없음. `6320750` 제목과 `14347b2` 본문에
      "자소서"만 있음. 전체 삭제보다 **메시지 2개 수정**을 권함. 공개 리포라 force push 필요 → **마지막에**
- [ ] **스키마 6줄 적용 시점** — 3단계에서 다중 그룹 테스트를 쓸 때 같이 적용하는 걸 권함
      (테스트가 있으니 뭐가 깨지는지 바로 보임)
- [ ] **`CLAUDE.md` 생성** — 다음에 다시 스캔 안 하려면 필요
- [ ] **브랜치 정리** — 지금 전부 `test/integration-test-foundation` 브랜치에서 진행 중.
      `main` 병합은 5단계 끝나고 한 번에

## 실행 환경 메모

이 맥은 자바가 Homebrew keg-only로 깔려 있어 PATH에 안 잡힙니다. 매번 이렇게 실행:
```
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
cd server && ./gradlew test
```

---

## 0단계 — 기반 (기능 코드 손대지 않음)

- [x] `build.gradle.kts`에 Testcontainers 추가 — **모듈명 주의**: 2.x부터
      `testcontainers-mysql`, `testcontainers-junit-jupiter` (1.x의 `mysql`, `junit-jupiter` 아님).
      버전은 Spring Boot 4.0.4 BOM이 관리 (2.0.4)
- [x] `application-test.yml` 분리 → `src/test/resources/`
- [x] MySQL 컨테이너에 `infra/init.sql` 물리기 — 복사본 없이 원본 참조.
      경로는 Gradle Test 태스크가 `schema.init.sql` 시스템 프로퍼티로 전달
- [x] MySQL·Redis 컨테이너 기동 확인 → `support/ContainerSmokeTest.kt` **3개 통과**

**결과: `./gradlew test` 4개 통과 (스킵 0).** `docker-compose`를 손으로 켤 필요 없음.

> **실행 시 주의**: 이 맥은 자바가 Homebrew keg-only로 깔려 있어 PATH에 안 잡힙니다.
> ```
> export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
> cd server && ./gradlew test
> ```

> **0단계 이전에 정말 안 돌았는가** — 확인함. 원래 설정 그대로 재현해 실행하니
> `java.net.ConnectException`(MySQL 연결 실패)로 죽음. 있던 테스트는
> `contextLoads()` 하나였고 본문이 비어 있어 아무것도 검증하지 않았음.
> `.env`도 이 폴더에 없음.

**H2 / 임베디드 Redis 금지.** 검증하려는 대상이 DB 엔진과 Redis의 실제 동작이라 대체하면 테스트가 의미를 잃습니다.

**테스트 프로파일에 `ddl-auto: create-drop`도 금지.** 같은 이유입니다.
검증 대상이 `uk_participate_group_user` UNIQUE 제약과 InnoDB row lock인데,
Hibernate가 생성한 스키마는 운영(`infra/init.sql`)과 미묘하게 다를 수 있습니다.
`validate` 유지 + 운영과 **같은 init.sql**을 씁니다.

### 잠복 — 스키마를 손으로 관리하고 있음

`ddl-auto: validate`는 스키마를 만들어주지 않으므로, 엔티티를 바꾸면 `infra/init.sql`을
손으로 같이 고쳐야 하고 안 고치면 부팅이 막힙니다. 이미 두 번 물린 흔적:
`b20f993`(init.sql을 엔티티에 맞게 수정), `f4166dc`(rank 예약어 백틱).

또 docker-compose의 init.sql은 **볼륨이 비어 있을 때 최초 1회만** 실행됩니다.
`mysql-data` 볼륨이 남아 있으면 init.sql을 고쳐도 반영 안 됨 → `docker compose down -v` 필요.

- [ ] 문서 `:29`가 "마이그레이션: Flyway"라고 써 있으나 **Flyway는 들어가 있지 않음** → 5단계에서 교정
- [ ] Flyway 도입 자체는 **하지 않음** (멘토: 기술 스택 늘리지 말 것). 이 항목은 "알고 있으나 안 한 선택"으로 남김

---

## 1단계 — 첫 테스트 하나

- [x] **동시 joinGroup**: 정원 1자리에 20명 동시 요청 → 정확히 1명만 성공
      - 대상: `GroupRepository.incrementMemberCounterIfNotFull()`
      - 증명 테스트: **`GroupJoinConcurrencyTest.onlyOneWinsTheLastSlot`**
- [x] **대조군**: 자리가 충분하면 20명 전원 성공 (앞 테스트가 고장난 게 아님을 보임)
      - 증명 테스트: **`GroupJoinConcurrencyTest.allSucceedWhenThereIsRoom`**

> **빨간불 확인함.** 구현을 TOCTOU 버전으로 일시 교체하니 `expected: 1 but was: 7`.
> 20명 중 7명이 통과했다. 원자적 UPDATE로 되돌리니 다시 1명.
> **초록불이 우연이 아님을 확인한 뒤 커밋함.**

이제 나머지 테스트는 이 구조를 복제하면 됩니다 —
`IntegrationTest` 상속 + `@BeforeEach` 자동 정리 + `CountDownLatch` 동시 실행.

---

## 2단계 — 버그 A (핵심)

### 무슨 일이 일어나고 있나

커밋을 가져올 때 서버가 이 순서로 일합니다.

1. 깃허브에서 커밋 **5개**를 받아온다
2. DB에 저장한다 — **이미 있는 건 안 넣는다**
3. 랭킹 점수를 올린다 — **그런데 그냥 "5점"을 더한다**

2번과 3번이 따로 놉니다. 2번에서 실제로 **0개**만 들어갔어도 3번은 5점을 더합니다.
버튼을 세 번 누르면 DB는 커밋 5개 그대로인데 랭킹은 **15점**이 됩니다.

### 스케줄러가 왜 안 고쳐주나

매시간 도는 스케줄러는 "DB를 세어보고 랭킹 점수를 맞추는" 일을 하는데,
**점수를 올리기만 하고 내리지는 않게** 되어 있습니다.

그렇게 만든 이유가 있습니다. 스케줄러가 DB를 세는 동안 사용자가 새 커밋을 올리면,
스케줄러가 방금 오른 점수를 옛날 숫자로 덮어써서 없애버립니다.
그걸 막으려고 **"지금 점수보다 클 때만 바꾼다"** 는 규칙을 넣었습니다.

```
랭킹 점수: 15점
DB 실제:    5개

스케줄러: "5는 15보다 작네. 안 바꿔."
→ 15점 그대로. 영원히.
```

**안전장치 두 개가 서로를 막고 있습니다.** 하나는 점수를 부풀리고, 다른 하나는 그걸 못 내리게 합니다.

### 왜 지금까지 몰랐나

부하 테스트용 가짜 깃허브가 **매번 새 커밋을 만들어냈습니다.**
중복이 생길 수가 없으니 이 버그가 나타날 방법이 없었습니다.

### 고치는 법

3번에서 "5개"가 아니라 **"실제로 들어간 개수"** 를 더합니다.
그러면 점수가 애초에 안 부풀어서 스케줄러가 내릴 일도 없어집니다.
**규칙("올리기만 한다")은 그대로 둡니다.** 그게 틀린 게 아니라 틀린 숫자를 넣은 게 문제였으니까요.

> 면접에서 강한 이유: "버그를 찾았다"가 아니라
> **"내가 넣은 안전장치 두 개가 서로를 방해하는 걸 발견했다"** 이기 때문입니다.
> 하나는 남이 봐도 찾지만, 둘의 상호작용은 자기 설계를 이해해야 보입니다.

---

**먼저 빨간불로 재현하고, 그 다음에 고쳤습니다.** (전부 완료)

- [x] **A-1. Mock을 실제와 같게** — `MockGithubClient`가 githubId/repositoryName 시드로
      결정론적 sha(SHA-1 40자 hex)를 반환하도록 교체. 같은 저장소는 항상 같은 커밋을 돌려줌
      - 증명 테스트: **`MockGithubClientTest`** (같은 저장소 → 같은 sha 등 3개)
- [x] **A-2. 빨간불 재현** — 같은 저장소를 두 번 수집 → 확인된 실패:
      `expected: 0 but was: 3` (두 번째 수집이 새 커밋 0건이어야 하는데 3건으로 잡힘)
      - 증명 테스트: **`CommitDuplicateFetchTest.fetchingSameCommitsTwiceDoesNotInflateRankScore`**
- [x] **A-3. 수정** — `CommitJdbcRepository.findExistingShas()`로 삽입 전 기존 sha 조회 후,
      `CommitService`가 실제 신규 커밋만 랭킹에 반영하도록 변경.
      (`batchUpdate` 반환값은 `rewriteBatchedStatements` 환경에서 `SUCCESS_NO_INFO(-2)`라
      건수 집계에 못 씀 → 사전 조회 방식 채택)
      `fetchAndSaveCommits()`의 반환값(POST /commits 응답 "saved")도
      요청 개수 → **실제 신규 저장 개수**로 의미 교정
- [x] **A-4. 월 버킷** — `YearMonth.now(clock)` → 신규 커밋을 `commitDate` 기준
      `YearMonth.from()`으로 그룹핑해 월별로 이벤트 분할 발행
      - A-3·A-4는 같은 커밋(위)이 증명. 위 테스트가 통과하면서 초록불 전환 확인

- [x] **A-5. 회귀 방지 테스트** — "GT는 점수를 못 내린다"는 사실 자체를 고정.
      나중에 누가 규칙을 되돌리지 못하게. 해당 코드: `RankingRedisRepository.kt:26-32`
      - 증명 테스트: **`RankingScoreHealTest`** (낮은 DB값 무시 / 높은 DB값 반영 / 신규 유저 채움, 3개)

---

## 3단계 — 나머지 테스트 (완료)

- [x] **스케줄러 정합성**: 스케줄러가 돌고 나면 Redis 점수 = DB 실제 개수
      - 증명 테스트: **`RankingSelfHealingSchedulerTest.schedulerHealsRedisScoreToMatchDb`**
- [x] **leaveGroup 카운터 드리프트** — 삭제 0건이어도 `decrementMemberCounter`를
      무조건 호출 → 비멤버의 탈퇴 요청으로 카운터가 내려가 정원 방어가 뚫림
      - 빨간불 확인: `Expecting code to raise a throwable.`
      - 증명 테스트: **`GroupLeaveTest`** (비멤버 예외 + 실멤버 정상 감소, 2개)
- [x] **분산 락 해제 순서** — 락 해제가 `@Transactional` 메서드 내부 `finally` →
      **커밋 전에 락이 풀림**. `CommitFetchLockReleaseEvent` +
      `@TransactionalEventListener(AFTER_COMPLETION)`으로 커밋/롤백 이후로 미룸
      - 증명 테스트: **`CommitFetchLockReleaseTest`** (예외 나도 락은 풀림 — 회귀 검증)
      - 정상 경로 검증: 커밋 전/후 나노초 단위 race는 sleep으로 신뢰성 있게
        재현 불가 → Spring의 `AFTER_COMPLETION` 계약으로 구조적 보장,
        `CommitDuplicateFetchTest`(연속 두 번 호출 성공)가 간접 증명
- [x] **Redis 장애 폴백** — `RedisConnectionFailureException`만 catch하던 걸
      `DataAccessException`으로 넓힘 (타임아웃 `QueryTimeoutException` 포함)
      - 빨간불 확인: 연결실패·정상 케이스는 기존 코드로도 통과, **타임아웃만 정확히 실패**
      - 증명 테스트: **`RankServiceFallbackTest`** (연결실패/타임아웃/정상, 3개)
- [x] **Clock 고정 테스트** — `Clock.fixed()`로 월 경계 커밋이 정확히 나뉘는지 확인
      - 증명 테스트: **`ClockInjectionTest`**
- [x] **다중 그룹 참여가 조용히 잘림** — `participations.first()`가 첫 그룹만 반환.
      다중 참여는 **요구사항**이라 스키마가 아니라 **앱이 버그**였음.
      구조적 수정(반환 타입 자체 변경)이라 빨간불 사이클 대신 코드 인스펙션으로 확인
      - 증명 테스트: **`GroupMultiMembershipTest`** (다중 그룹 전부 반환 + 무소속 빈 목록, 2개)

### 조용히 고친 것 — 그중 하나가 진짜 API 버그였음

- [x] `RankService.kt` — `private fun`에 `@Transactional`. self-invocation이라
      AOP 프록시가 못 잡아 애초에 무효. 제거 (JpaRepository가 자체 트랜잭션 보유)
- [x] `GroupService.createGroup` — `joinGroup`과 순서 통일(증가 먼저, 확인 후 참여 기록).
      `maxCount=0` 같은 값을 `GroupFullException`으로 막음
- [x] **`createGroup` 응답의 `memberCount`가 항상 0으로 나감** — 원래 코드의 실제 API 버그.
      `incrementMemberCounterIfNotFull()`은 벌크 UPDATE라 DB만 바꾸고 Hibernate 세터를
      안 거치므로, `return group`이 증가 전 값(0)을 그대로 내보냈다.
      그룹을 만들면 본인이 자동 멤버가 되는데도 응답은 "멤버 0명"이었음.
      "잠복"이라고 적어뒀던 게 사실은 눈에 보이는 버그였다.
      - 빨간불 확인: `expected: 1 but was: 0`
      - 증명 테스트: **`GroupCreateResponseTest`**
      - 수정: 증가 후 `findById`로 재조회해서 반환.
        이때 1차 캐시가 아니라 DB를 읽게 하려고 `@Modifying`에
        `clearAutomatically = true`가 필요했음

> **아래는 원래 코드의 결함이 아니라, 위 수정 중 이 세션에서 새로 만든 회귀입니다.**
>
> 원래 `@Modifying`에는 아무 옵션도 없었고 정상 동작했다.
> `clearAutomatically`를 넣으면서 `flushAutomatically`를 빠뜨렸더니,
> 아직 DB에 안 쓰인 `deleteByGroupAndUser`의 삭제가 `clear()`에 통째로 버려져
> `GroupLeaveTest`가 깨졌다 (`[45L, 46L]` — 지워졌어야 할 참가자가 남음).
> 두 벌크 UPDATE는 `groups` 테이블만 건드려서 `participate`의 대기 삭제와
> query space가 겹치지 않아 Hibernate 자동 flush도 일어나지 않았다.
> `flushAutomatically = true`를 같이 넣어 해결.
>
> **레거시 결함 목록에 넣지 말 것.** 테스트가 있어서 즉시 걸린 사례일 뿐이다
> — 오히려 3단계에서 쌓은 테스트가 값을 한 증거로 쓰는 게 맞다.

---

## 4단계 — 깨끗한 측정 하나 (완료)

- [x] 변수 **하나만** 바꾼 A/B: **`ranking.redis.enabled`** on/off
      - `db` 팔은 별도 구현이 아니라 **운영의 Redis 장애 폴백 경로**를 강제로 태운 것
      - 덕분에 측정 하나가 "왜 Redis로 옮겼나"와 "Redis 죽으면?" 둘 다 답함
- [x] 규모를 바꿔가며 **기울기**: 유저 1만 / 5만 / 10만
- [x] 표 + 결과 파일
      - 결과: **[측정-결과.md](측정-결과.md)**, 원본 **[`bench/results/`](../bench/results/)**

**핵심 숫자** — 유저 10만에서 `GET /rank`:

| | 처리량 | 실패율 | 단일 요청(경합 없음) |
|---|---|---|---|
| `redis` | 13,692 req/s | 0% | 2.6 ms |
| `db` | 6.1 req/s | 2.84% | 464.9 ms |

격차가 규모와 함께 벌어짐: **18배 → 78배 → 179배** (1만 → 5만 → 10만)

> **1차 측정을 버리고 다시 쟀습니다.** 처음엔 "2,001배"였는데, 그때의
> `findTop30Rank`가 아무도 안 읽는 컬럼 3개를 조회하고 있었습니다.
> **Redis를 군살 붙은 쿼리와 비교한 셈**이라 공정하지 않았습니다.
> 쿼리를 고치니(`6b6674d`) 8,005ms → 464.9ms. 서사도 바뀝니다 —
> DB 경로는 "무너지는" 게 아니라 "느려지는" 것이었습니다.
>
> 그리고 `DENSE_RANK`는 범인이 아니었습니다. 윈도우 함수 비용은 **9ms, 전체의 2%**.

### 측정 중 걸린 것 두 개 (문서에 기록함)

1. **`db` 팔의 p95를 지연 지표로 쓸 수 없었음** — 50k/100k에서 HikariCP 풀(20개)이
   포화돼 대부분 3초 타임아웃으로 실패. `min=3s`, `med=3.01s`가 쿼리 시간이 아니라
   `connection-timeout`이 찍힌 것. p95는 "느린 성공 + 빠른 실패"의 혼합값
2. **평균값의 함정** — `db` 평균이 5만 9.52초 → 10만 8.71초로 *줄어듦*.
   빨라진 게 아니라 실패가 늘어 3초짜리 빠른 실패가 평균을 끌어내린 것

→ "그건 풀 크기 문제 아니냐"는 반론을 막으려고 **동시성 1 보조 측정**을 추가.
경합이 없는데도 Top30이 1만에서 1.5초, 10만에서 8초. **풀 포화는 원인이 아니라 결과.**

기존 `k6_result*.json` 3개는 변수가 최소 4개 섞여 있어 A/B가 아니었음 → 5단계에서 문서 정리 시 함께 처리.

---

## 5단계 — 문서 하나 (완료)

`docs/spring-migration-plan.md` (445줄) → **[마이그레이션-기록.md](마이그레이션-기록.md)** 로 재작성.
("계획"이 아니라 "기록"이 됐으므로 `git mv`로 이름도 교정 — 이력 보존)

- [x] 자소서 문단 삭제 (헤더 `:3`, "최종 자소서 한 문단" 절)
- [x] 면접 답변 블록 5개 + 예상 질문 표 9행 삭제
- [x] **시점 교정** — 인덱스 무력화·시점 계산은 **Flask 단계에서 고친 것**(커밋 `0bd802f`).
      "① Flask 단계 / ② Kotlin 전환 / ③ 테스트 도입 이후" 3시기로 분리해 서술.
      `:11`의 "4,234ms → 86ms"도 **Flask 성과**임을 명시
- [x] **오래된 인용 삭제** — `WHERE YEAR(commit_date)`는 현재 코드에 없음.
      실제 레거시였던 `extract(year FROM ...)` 형태로 교정
- [x] **p95 서사 교정** — 변수 4개 섞인 Flask vs Spring 비교표를 통째로 걷어내고,
      변수 하나만 바꾼 새 측정([측정-결과.md](측정-결과.md))으로 대체
- [x] **DENSE_RANK 서술 교정** — "O(N log N) 병목"이라 적었으나 실측 결과 윈도우 함수 비용은
      **9ms, 전체의 2%**. 진짜 비용은 매 요청 전체 집계임
- [x] **Flyway 정정** — 스택 표에 "Flyway"라고 써 있었으나 도입 안 함. 미도입 사실과 근거를 명시
- [x] 각 결함에 **증명 테스트 이름**을 걸어 문서와 코드를 묶음 (12개 전부 실재 확인)
- [x] "알고 있으나 하지 않은 것" 절 신설 — Flyway / `DENSE_RANK` 등수 압축 /
      Redis 장기 장애 대책 / 인증 없음

---

## 이야기 세 개 (꼬리질문 3개를 버티는가)

**세 개는 서로 다른 서랍입니다. 섞이면 신뢰를 잃습니다.**
① 랭킹 **성능** / ② 랭킹 **정합성** / ③ 그룹 **정원**

### ① Redis 랭킹 오프로딩 (성능)

읽기 비용을 쓰기로 옮긴 것. ZSET은 **캐시가 아니라 정렬을 유지하는 자료구조**다
(스킵리스트 + 해시). 점수가 바뀌면 그 한 명만 `O(log N)`으로 자리를 옮긴다 — 전체 재정렬이 없다.

- **"Redis가 죽으면요?"** → 답 있음. 1만 견딤(102 req/s, 실패 0%) / 10만 한계(6.1 req/s, 2.84% 실패).
  **"짧은 장애는 버티지만 길어지면 대책 없음"** 이 정확한 답
- **"집계 테이블 두면 되잖아요?"** ← **가장 위험한 꼬리질문.** 준비된 답:
  *"맞습니다. 이 규모에선 그쪽이 더 단순하고 안전했을 겁니다.
  진실의 원천이 하나라 ②번 문제가 애초에 안 생기고요.
  Redis를 고른 건 이기종 저장소 정합성을 다뤄보려는 학습 목적이 컸습니다."*
  → 근거: [마이그레이션-기록.md](마이그레이션-기록.md) "집계 테이블 대신 Redis를 쓴 것"

### ② ★ 안전장치 두 개가 서로를 무력화 (정합성) — 가장 강한 이야기

**랭킹 점수 이야기다. `member_counter`·TOCTOU와 무관하다.**

자가 치유 스케줄러 안의 GT 규칙이 자가 치유 자체를 반쪽으로 만들었다.

```
① 요청 개수를 그대로 더함 → 점수가 부품
② GT("클 때만 갱신")가 하향 보정을 막음 → 영구화

①만 있으면 스케줄러가 1시간 안에 고침. ②만 있으면 부풀 일이 없음. 둘 다여서 영구 오류.
```

- **"왜 GT를 넣었나"** → 배치가 실시간 증분을 덮어쓰는 걸 막으려고
- **"그럼 하향 보정은"** → GT를 빼는 게 아니라 **증분을 정확하게** 만들어 부풀지 않게 함
- **"왜 못 찾았나"** → Mock이 매번 새 UUID를 뱉어 중복이 구조적으로 발생 불가했음

### ③ member_counter 3단계 시행착오 (정원)

컬럼 제거(OOM) → COUNT 후 INSERT(TOCTOU) → 원자적 조건부 UPDATE.

**두 가지 깨지는 방식을 구분해서 말할 것:**

| | 무엇이 틀리나 |
|---|---|
| Flask 원본 | A·B가 동시에 4를 읽고 둘 다 5를 씀 → **카운터가 실제보다 작음** |
| 재현용 TOCTOU 버전 | DB는 거절(0 반환)했는데 코드가 무시 → **참가자 행이 정원 초과** |

직접 재현한 결과: `expected: 1 but was: 8` (실행할 때마다 숫자가 달라짐)

## 얻지 말 것

- "Kotlin/Spring 할 줄 안다"는 성과가 아니라 **진입 조건**. 성과로 말하면 수준이 낮아 보임
- **기술 스택 추가 금지** — Kafka, MSA, 서킷브레이커. 넣는 순간 "왜 넣었냐"에 답해야 함
